# 中新数据通道 PDPA 合规 VLM 推理 API 依赖清单
# 安装命令：pip install -r requirements.txt

# ─── 核心 Web 框架 ───────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# ─── 数值计算与机器学习 ──────────────────────────────────────────
numpy>=1.26.0
torch>=2.2.0
torchvision>=0.17.0
Pillow>=10.2.0

# ─── 国密算法（中国商用密码标准） ───────────────────────────────
gmssl>=3.2.2                   # SM2/SM3/SM4 原生实现
cryptography>=42.0.0           # 开发模式回退 + TLS 基础设施

# ─── HTTP 客户端（边缘端） ───────────────────────────────────────
requests>=2.31.0
urllib3>=2.2.0

# ─── JWT / OIDC 身份验证 ─────────────────────────────────────────
python-jose[cryptography]>=3.3.0
authlib>=1.3.0

# ─── VLM 推理引擎（重庆 AIDC 云端，GPU 环境） ────────────────────
# vllm>=0.4.0                  # 取消注释以启用真实推理（需要 CUDA 环境）

# ─── 日志与监控 ──────────────────────────────────────────────────
structlog>=24.1.0

# ─── 开发与测试工具 ──────────────────────────────────────────────
pytest>=8.1.0
pytest-asyncio>=0.23.0
httpx>=0.27.0                  # FastAPI 测试客户端
