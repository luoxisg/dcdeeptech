version: "3.9"

services:
  compliance:
    build:
      context: ..
      dockerfile: Dockerfile
    ports:
      - "8081:8081"
    env_file:
      - ../.env.local
    volumes:
      - compliance-data:/app/data
    restart: unless-stopped

volumes:
  compliance-data:
